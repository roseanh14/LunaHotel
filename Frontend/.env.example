# Copy to .env.development or .env.local
# true  = use built-in demo rooms, services, and bookings (no Java server needed)
# false = call the real API (Spring Boot on VITE_API_URL or default localhost:4040)
VITE_USE_MOCK_DATA=false
